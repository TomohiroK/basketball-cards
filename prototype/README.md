# Prototype Snapshot

This directory preserves the open-information prototype UI before the production-facing
`You vs NPC` interface work.

- `App.prototype.tsx`: previous all-cards-visible React UI
- `styles.prototype.css`: matching styles from that snapshot

The active app continues to use `src/App.tsx` and `src/styles.css`.
